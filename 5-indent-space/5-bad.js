/**
 * Principles: Indent & Space Your Code
 */

const number = 1;
const arrayOfNumber = [1,2,3];
function findIndex(num, array){
array.forEach((value, index) => {
if (num === value) {
return index;
}else{
continue;
}})}
findIndex(integer,arrayOfNumber);